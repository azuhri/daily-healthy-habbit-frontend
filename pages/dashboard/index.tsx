import type {
  InferGetStaticPropsType,
  GetStaticProps,
  GetServerSideProps,
} from "next";
import $ from "jquery";

export default function DashboardPage() {
  return <div className="min-h-[100vh] bg-white">ini dashboard</div>;
}
